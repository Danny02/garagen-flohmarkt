name = "garagen-flohmarkt-worker"
main = "src/index.ts"
compatibility_date = "2024-12-30"

# ── KV Namespace ──────────────────────────────────────────────────────────────
# Run `npm run kv:create` once to get the IDs, then fill them in below.
# The GitHub Actions deploy workflow injects CLOUDFLARE_KV_NAMESPACE_ID
# and CLOUDFLARE_KV_PREVIEW_ID as secrets.
[[kv_namespaces]]
binding  = "GARAGEN_KV"
id       = "REPLACE_WITH_KV_NAMESPACE_ID"
preview_id = "REPLACE_WITH_KV_PREVIEW_ID"

# ── Optional: Admin token (set as a Worker secret via wrangler secret put) ───
# [vars]
# ADMIN_TOKEN = ""   # set with: wrangler secret put ADMIN_TOKEN
